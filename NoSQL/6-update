// Update documents in the "school" collection to add a new attribute
db.school.updateMany(
    { name: "Holberton school" },  // Filter for matching documents
    { $set: { address: "972 Mission street" } }  // Set the new attribute
)
